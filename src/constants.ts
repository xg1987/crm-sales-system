import { Customer, Activity, Stats } from './types';

export const MOCK_STATS: Stats = {
  totalStudents: 0,
  purchaseIntent: '中',
  followUpsToday: 0,
};

export const MOCK_CUSTOMERS: Customer[] = [];

export const MOCK_ACTIVITIES: Activity[] = [];
